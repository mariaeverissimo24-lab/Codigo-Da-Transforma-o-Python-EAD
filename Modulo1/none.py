def calcular_media(lista_numeros):
    if not lista_numeros:
        return sum(lista_numeros)\ len(lista_numero)
    
    def e_par(numero):
        return numero % 2 == 0
    def divisao_inteira (a,b): 
        """
        Retorna